<div class="text-center">
<h1>ルートを作成</h1>
<%= form_for(@route) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
  
    <%= f.label :content %>
    <%= f.text_area :content %>
    
    <%= f.submit "作成", class: "btn btn-secondary" %>
<% end %>
</div>